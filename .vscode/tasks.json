{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build test",
            "command": "zig test -femit-bin=zig-out/bin/${fileBasenameNoExtension}-test --test-no-exec ${file}"
        },
    ]
}